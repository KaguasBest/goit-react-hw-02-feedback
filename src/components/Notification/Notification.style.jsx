import styled from 'styled-components';

export const NotiWrap = styled.div({
  textAlign: 'center',
  background: '#d8d7d7',
  margin: 'auto',
  width: 960,
  padding: 40,
});

export const Text = styled.p({
  fontSize: 26,
  fontWeight: 700,
  color: '#333',
});
