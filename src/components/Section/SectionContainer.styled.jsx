import styled from 'styled-components';

export const Container = styled.div({
  background: '#d8d7d7',
  margin: 'auto',
  width: 960,
  padding: 40,
});

export const Title = styled.h1({
  fontSize: 30,
  textAlign: 'center',
  marginBottom: 30,
});
